function numbersFromNTo1(input) {
    const numA = Number(input[0]);

    for (let i = numA; i > 0; i--) {
        console.log(i);
    }
}
numbersFromNTo1(['2']);
numbersFromNTo1(['3']);
numbersFromNTo1(['5']);